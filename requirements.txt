# Concert Ticket Price Monitor — Python Dependencies
# Install with: pip install -r requirements.txt

# HTTP client (async-ready, type-friendly)
httpx>=0.27.0

# Retry / backoff
tenacity>=8.2.3

# HTML parsing
beautifulsoup4>=4.12.3
selectolax>=0.3.21   # faster HTML parser (optional, used in fallback scrapers)

# Scheduler
APScheduler>=3.10.4

# Config
PyYAML>=6.0.1

# CLI + terminal UI
typer[all]>=0.12.3
rich>=13.7.1

# Headless browser for JS-rendered sites (StubHub)
playwright>=1.40.0

# Telegram notifications (optional but recommended)
# Uncomment if using Telegram:
# python-telegram-bot>=21.0
